import React from 'react';
import styled from 'styled-components';

export default function header() {
  return <styledHeader>Peter Lofland</styledHeader>;
}

const styledHeader = styled.div`
  font-size: 3rem;
`;
